 apiVersion: networking.k8s.io/v1	
 kind: 	NetworkPolicy
 
 metadata:
  name: nvp
  namespace:	dev
 
 spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: nginx

    ingress:
      ports:
        port:80